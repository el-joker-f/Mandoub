import { initializeApp, getApps } from "https://www.gstatic.com/firebasejs/12.17.1/firebase-app.js";
import { getFirestore, collection, getDocs } from "https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js";
const config={apiKey:"AIzaSyB6-RVH7-8NrN-AaOOv6Qj9APDeyj7oIU",authDomain:"mandoub-dv.firebaseapp.com",projectId:"mandoub-dv",storageBucket:"mandoub-dv.firebasestorage.app",messagingSenderId:"311140400335",appId:"1:311140400335:web:db198b7c53259c53594bba"};
const app=getApps().length?getApps()[0]:initializeApp(config);const db=getFirestore(app);const esc=v=>String(v??"").replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));const date=v=>v?.toDate?v.toDate():(v?new Date(v):null);const active=o=>o.active!==false&&(!date(o.startAt)||new Date()>=date(o.startAt))&&(!date(o.endAt)||new Date()<=date(o.endAt));
async function load(){const host=document.getElementById("homeOffersList");if(!host)return;try{const snap=await getDocs(collection(db,"offers"));const offers=snap.docs.map(d=>({id:d.id,...d.data()})).filter(o=>o.showOnHome!==false&&active(o));if(!offers.length){host.closest("section")?.remove();return;}host.innerHTML=offers.map(o=>`<article class="home-offer-card"><img src="${esc(o.image||'assets/logo.png')}" alt="${esc(o.title)}" onerror="this.src='assets/logo.png'"><div class="home-offer-body"><span class="home-offer-badge">🎁 عرض</span><h3>${esc(o.title)}</h3><p>${esc(o.description||'عرض مميز من مندوب')}</p><div class="home-offer-price">${Number(o.price||0).toLocaleString('ar-EG')} ج ${Number(o.oldPrice||0)>Number(o.price||0)?`<del>${Number(o.oldPrice).toLocaleString('ar-EG')} ج</del>`:''}</div>${Number(o.discount||0)>0?`<small>خصم ${Number(o.discount)}%</small>`:''}</div></article>`).join('');}catch(e){console.error('Home offers error',e);}}
load();