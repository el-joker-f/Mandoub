/* Restaurants: service type + food category + search + favorites + delivery */
import{auth,db as userDb}from'./auth.js';import{getDoc,doc,setDoc,serverTimestamp}from'https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js';
(async function(){const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));const wait=()=>new Promise(resolve=>{if(window.firebaseReady)return resolve(true);let n=0;const t=setInterval(()=>{if(window.firebaseReady){clearInterval(t);resolve(true)}else if(++n>60){clearInterval(t);resolve(false)}},100)});if(!(await wait()))return;const db=window.firebaseDB,col=window.firebaseCollection,getDocs=(await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-firestore.js')).getDocs;let restaurants=[],favorites=[];async function loadFav(){const u=auth.currentUser;if(!u){favorites=JSON.parse(localStorage.getItem('mandoub_fav_restaurants')||'[]');return}try{const s=await getDoc(doc(userDb,'customers',u.uid));favorites=s.exists()?(s.data().favoriteRestaurants||[]):[]}catch{favorites=[]}}async function toggleFav(id){const u=auth.currentUser;if(!u){const a=new Set(JSON.parse(localStorage.getItem('mandoub_fav_restaurants')||'[]'));a.has(id)?a.delete(id):a.add(id);localStorage.setItem('mandoub_fav_restaurants',JSON.stringify([...a]));favorites=[...a];render();return}const a=new Set(favorites);a.has(id)?a.delete(id):a.add(id);favorites=[...a];try{await setDoc(doc(userDb,'customers',u.uid),{favoriteRestaurants:favorites,updatedAt:serverTimestamp()},{merge:true});render()}catch(e){console.error(e)}}window.toggleRestaurantFavorite=toggleFav;try{const s=await getDocs(col(db,'restaurants'));restaurants=s.docs.map(d=>({id:d.id,...d.data()})).filter(r=>r.active!==false)}catch(e){console.error(e);document.getElementById('restaurantsContainer').innerHTML='<div class="restaurants-message">❌ تعذر تحميل الأقسام</div>';return}await loadFav();const container=document.getElementById('restaurantsContainer'),search=document.getElementById('search'),typeFilters=document.querySelectorAll('#typeFilters .filter'),foodFilters=document.getElementById('foodFilters');const food=[...new Set(restaurants.map(r=>r.foodCategory||r.category).filter(Boolean))];foodFilters.innerHTML='<button class="filter active" data-food="all">كل أنواع الأكل</button>'+food.map(x=>`<button class="filter" data-food="${esc(x)}">${esc(x)}</button>`).join('');let type=new URLSearchParams(location.search).get('type')||'all',foodType='all';function setActive(){typeFilters.forEach(b=>b.classList.toggle('active',b.dataset.type===type));foodFilters.querySelectorAll('.filter').forEach(b=>b.classList.toggle('active',b.dataset.food===foodType))}function render(){const q=(search?.value||'').trim().toLowerCase();const list=restaurants.filter(r=>(type==='all'||(r.businessType||'restaurant')===type)&&(foodType==='all'||(r.foodCategory||r.category||'')===foodType)&&(!q||`${r.name} ${r.category||''} ${r.foodCategory||''}`.toLowerCase().includes(q)));container.innerHTML=`<div class="restaurant-picker">${list.map(r=>`<div class="restaurant-choice"><a href="restaurant-menu.html?id=${encodeURIComponent(r.id)}" style="display:flex;align-items:center;gap:12px;flex:1;color:inherit;text-decoration:none"><img src="${esc(r.image||'assets/logo.png')}" onerror="this.onerror=null;this.src='assets/logo.png'" alt="${esc(r.name)}"><div style="flex:1"><strong>${esc(r.name)}</strong><small>${esc(r.foodCategory||r.category||'مطعم')} ${r.rating?`★ ${esc(r.rating)}`:''}</small><small class="delivery-preview" data-id="${esc(r.id)}">🚚 حساب التوصيل حسب موقعك</small></div></a><button onclick="toggleRestaurantFavorite('${esc(r.id)}')" style="border:0;background:transparent;color:${favorites.includes(r.id)?'#ff5c87':'#91a4b3'};font-size:25px;cursor:pointer" title="مفضلة">${favorites.includes(r.id)?'♥':'♡'}</button></div>`).join('')||'<div class="restaurants-message">لا توجد نتائج مطابقة.</div>'}</div>`;renderDelivery()}function renderDelivery(){document.querySelectorAll('.delivery-preview').forEach(e=>{const r=restaurants.find(x=>x.id===e.dataset.id);const d=window.getMandoubDelivery?.(r);if(d?.fee===-1)e.textContent=`🚫 ${d.distance.toFixed(1)} كم — خارج نطاق التوصيل`;else if(d?.fee!=null)e.textContent=`🚚 ${d.fee} ج • ${d.distance.toFixed(1)} كم`;else e.textContent='🚚 حدد موقعك لمعرفة رسوم التوصيل'})}typeFilters.forEach(b=>b.onclick=()=>{type=b.dataset.type;setActive();render()});foodFilters.addEventListener('click',e=>{const b=e.target.closest('.filter');if(!b)return;foodType=b.dataset.food;setActive();render()});search?.addEventListener('input',render);setActive();render();window.addEventListener('mandoubLocationChanged',renderDelivery);window.addEventListener('mandoubCartChanged',renderDelivery);onAuthStateChangedSafe();async function onAuthStateChangedSafe(){try{const{onAuthStateChanged}=await import('https://www.gstatic.com/firebasejs/12.17.1/firebase-auth.js');onAuthStateChanged(auth,async()=>{await loadFav();render()})}catch{}}})();